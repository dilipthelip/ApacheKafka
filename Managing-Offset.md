## Managing Offset:

In this section we will discuss about how kafka manages/maintains the offset.

### Offset in Kafka Topic

- Every topic can have number of partitions.
- Every partition maintains its own offset.

